# Slim Pipeline — Voice Feature Extraction (no LLM, no transcription)

# Core audio processing
librosa>=0.10.0
soundfile>=0.12.1
audioread>=3.0.0
numpy>=1.24.0
scipy>=1.10.0

# Machine learning (MERaLiON-SER-v1 emotion model)
torch>=2.0.0
torchaudio>=2.0.0
transformers>=4.35.0
peft>=0.18.0

# Acoustic features (eGeMAPS via openSMILE)
opensmile>=2.5.0

# Voice quality (jitter, shimmer, HNR via Praat)
praat-parselmouth>=0.4.3

# Utilities
python-dotenv>=1.0.0
pydantic>=2.5.0
rich>=13.0.0

# API server
fastapi>=0.109.0
uvicorn>=0.27.0
python-multipart>=0.0.6
